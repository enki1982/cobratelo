echo "=== ESTADO DEL ENRICHER ==="
ps aux | grep enricher.py | grep -v grep || echo "No está corriendo"
echo ""
echo "=== ÚLTIMAS LÍNEAS DEL LOG ==="
tail -30 /root/enricher.log 2>/dev/null || echo "Sin log todavía"
